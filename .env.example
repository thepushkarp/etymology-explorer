# Etymology Explorer Environment Variables

# ===========================================
# Anthropic (required)
# ===========================================
# Server-side key only. Never expose in browser code.
ANTHROPIC_API_KEY=
# Optional override. Must stay in Claude Haiku 4.5 family.
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# ===========================================
# Upstash KV (recommended - for cache + limits + cost guard)
# ===========================================
ETYMOLOGY_KV_REST_API_URL=
ETYMOLOGY_KV_REST_API_TOKEN=

# ===========================================
# Turnstile challenge verification (optional but recommended)
# ===========================================
# Public site key is safe for browser exposure and required for challenge flows.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ===========================================
# Signed identity for authenticated rate-limit tier (optional)
# ===========================================
# If set, requests can only enter authenticated tier when cookies
# `user-id` and `user-id-sig` are present and HMAC-verified.
REQUEST_IDENTITY_SIGNING_SECRET=

# ===========================================
# Request trust + admin operations
# ===========================================
# Keep false unless this app is behind a trusted proxy/CDN that sets
# canonical client IP headers (cf-connecting-ip / x-forwarded-for).
TRUST_PROXY_HEADERS=false
# Required to access GET /api/admin/stats via header x-admin-secret.
# Use a long random value (minimum 16 chars).
ADMIN_SECRET=

# ===========================================
# Operator feature flags (optional)
# ===========================================
PUBLIC_SEARCH_ENABLED=true
FORCE_CACHE_ONLY=false
DISABLE_PRONUNCIATION=false
CSP_REPORT_ONLY=true

# ===========================================
# ElevenLabs TTS (optional - for pronunciation audio)
# ===========================================
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
